# AGENTS.md

This repository's operational guidance is in [CLAUDE.md](./CLAUDE.md).

It documents:

- the project mission and content pipeline,
- the routing model and frontmatter contract,
- the markdown processing chain (wikilinks, image embeds, math),
- search architecture and Czech-aware tokenization,
- the theming layer (`@theme` tokens + `app.config.ts` semantic roles),
- commands, pre-commit/pre-push policy, content validation,
- deferred items and non-goals.

Read it before making changes. Conventions there bind any agent (Claude or
otherwise) operating on this repo.
